@model BookReadingEvents.ViewModels.LoginViewModel
@{
    Layout = "~/Views/Shared/_LayoutLoginSignup.cshtml";
}
<div class="row">
    <div class="col"></div>
    <div class="col login-form mt-5">
        <h1 class="head" style="position:relative; right:-10px;">Login</h1>
        <br />
        <form action="/login/index" method="POST">
            <div class="form-group">
                @Html.LabelFor(model => model.Email, htmlAttributes: new { @class = "control-label col-md-2" })
                @Html.EditorFor(model => model.Email)
            </div>

            <div class="form-group">
                @Html.LabelFor(model => model.Password, htmlAttributes: new { @class = "control-label col-md-2" })
                @Html.PasswordFor(model => model.Password)
            </div>

            <button type="submit" style="position:relative; right:-10px;" class="btn btn-primary">Submit</button>
            <div class="line"></div>
            <br />
            <div style="position:relative; right:-10px;">Don't have an account ? @Html.ActionLink("Sign Up", "SignUp", "Login")</div>
        </form>
    </div>
    <div class="col"></div>
</div>

